# docs/

| Folder | Contents |
|---|---|
| `design/` | Current engineering records. These supersede `notes/` where they disagree. |
| `reference/` | Monaco 2026 rules, notice of challenge, cell datasheet. Read-only source material. |
| `decisions/` | Architecture decision records: why the repository is shaped the way it is. |

The original design discussion lives in `notes/` at the repository root, not
here. It is partly superseded; see `notes/README.md`.
